<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activityexperiment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActivityExperiment">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".activity.FirstActivity"></activity>
        <activity
            android:name=".activity.SingleTopAcitivty"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.SingleTaskAcitivty"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.SingleInstanceAcitivty"
            android:launchMode="singleInstance" />

        <activity android:name=".activity.StablefActivity" />
        <activity android:name=".activity.DynamicfActivity" />
        <activity android:name=".activity.ShareMessageActivity" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>